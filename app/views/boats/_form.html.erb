<%= form_for(@boat) do |f| %>
  <% if @boat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@boat.errors.count, "error") %> prohibited this boat from being saved:</h2>

      <ul>
      <% @boat.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %><br />
    <%= f.text_field :number %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :Baad_type %><br />
	<%= f.select :boat_type_id, options_for_select(@boat_types) %>
  </div>
  <div class="field">
    <%= f.label :Udstyr %><br />
	<%= f.select :equipments, options_for_select(@equipments),{},{:multiple=>true} %>
  </div>
  <div class="field">
    <%= f.label :Sejl %><br />
	<%= f.select :sails, options_for_select(@sails), {} ,{:multiple=>true}%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
